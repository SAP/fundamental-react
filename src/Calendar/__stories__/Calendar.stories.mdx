import Calendar from '../Calendar';
import { dateKnobToDate } from '../../../.storybook/custom/utils';
import moment from 'moment';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import {
    boolean,
    date,
    optionsKnob,
    text,
} from '@storybook/addon-knobs';

<Meta title="Component API/Calendar" component={Calendar} />

# Calendar

The **Calendar** is commonly used as the contents of a **Popover** when composing a **Date Picker**.
It is rarely used on its own as a standalone component.

```javascript
 import { Calendar } from 'fundamental-react/Calendar';
 ```

## Examples

### Default
<Preview withToolbar>
  <Story name='Default'>
        <Calendar />
  </Story>
</Preview>

### Disabled Weekends and Disabled Before Date
<Preview withToolbar>
  <Story name='disableWeekends + disableBeforeDate'>
        <Calendar
            disableBeforeDate={new Date()}
            disableWeekends />
  </Story>
</Preview>

### Blocked Dates 
<Preview withToolbar>
  <Story name='Blocked dates'>
        <Calendar
            blockedDates={[new Date(2018, 1, 1, 0, 0, 0, 0), new Date(2018, 3, 3, 0, 0, 0, 0)]}
            disableWeekday={['Monday', 'Tuesday']} />/>
  </Story>
</Preview>

### Special Days
<Preview withToolbar>
  <Story name='Special Days'>
        <Calendar
            specialDays={{
                [moment().add(1, 'day').endOf('day').format('YYYYMMDD')]: 1,
                [moment().add(2, 'day').endOf('day').format('YYYYMMDD')]: 2,
                [moment().add(3, 'day').endOf('day').format('YYYYMMDD')]: 3,
                [moment().add(7, 'day').endOf('day').format('YYYYMMDD')]: 4
            }}/>
  </Story>
</Preview>

### Range Selection
<Preview withToolbar>
  <Story name='Range Selection'>
        <Calendar enableRangeSelection />
  </Story>
</Preview>


## Properties
<Props of={Calendar} />


<!-- Stories excluded from documentation page -->

<Story name='Dev' parameters={{ docs: { disable: true }}}>
    <Calendar
            blockedDates={[dateKnobToDate('block between dates (1)', new Date(new Date().getFullYear(), new Date().getMonth() - 1, 1)),
                dateKnobToDate('block between dates (2)', new Date(new Date().getFullYear(), new Date().getMonth() - 1, 10))]}
            disableAfterDate={dateKnobToDate('disable after date', new Date(new Date().getFullYear() + 1, 0, 1))}
            disableBeforeDate={dateKnobToDate('disable before date', new Date(new Date().getFullYear() - 1, 0, 1))}
            disabledDates={[dateKnobToDate('disable between dates (1)', new Date(new Date().getFullYear(), new Date().getMonth() - 1, 15)),
                dateKnobToDate('disable between dates (2)', new Date(new Date().getFullYear(), new Date().getMonth() - 1, 25))]}
            disableFutureDates={boolean('disable future dates', false)}
            disablePastDates={boolean('disable past dates', false)}
            disableWeekday={optionsKnob('disable weekdays', 
                {
                    Sunday: 'Sunday',
                    Monday: 'Monday',
                    Tuesday: 'Tuesday',
                    Wednesday: 'Wednesday',
                    Thursday: 'Thursday',
                    Friday: 'Friday',
                    Saturday: 'Saturday'
                },
                null,
                { display: 'check' })}
            disableWeekends={boolean('disable weekends', false)}
            locale={text('locale', 'en')} />
</Story>
