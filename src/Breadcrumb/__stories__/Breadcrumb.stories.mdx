import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Breadcrumb from '../Breadcrumb';

<Meta title="Component API/Breadcrumb" component={Breadcrumb} />

# Breadcrumb

The **Breadcrumb** allows users to see the current page and navigation path to that page. Users can navigate
to previous levels in the path. When clicking on the current page, a dropdown allows users to access
other pages at that same level.

```javascript
 import { Breadcrumb } from 'fundamental-react/Breadcrumb';
 ```

## Examples

### Default 
<Preview withToolbar>
  <Story name='Default'>
       <Breadcrumb>
            <Breadcrumb.Item name='Link Text' url='#' />
            <Breadcrumb.Item name='Link Text' url='#' />
            <Breadcrumb.Item name='Link Text' url='#' />
        </Breadcrumb>
  </Story>
</Preview>

### Using Url Prop
<Preview withToolbar>
  <Story name='Using Url Prop'>
      <Breadcrumb>
        <Breadcrumb.Item name='Link Text' url='#' />
        <Breadcrumb.Item name='Link Text' url='#' />
        <Breadcrumb.Item name='Link Text' url='#' />
    </Breadcrumb>
  </Story>
</Preview>

## Properties

### Breadcrumb
<Props of={Breadcrumb} />

### Breadcrumb.Item
<Props of={Breadcrumb.Item} />

<!-- Stories excluded from documentation page -->

<Story name='Disabled Styles' parameters={{ docs: { disable: true }}}>
    <Breadcrumb disableStyles>
        <Breadcrumb.Item name='Link Text' url='#' />
        <Breadcrumb.Item name='Link Text' url='#' />
        <Breadcrumb.Item name='Link Text' url='#' />
    </Breadcrumb>
</Story>
