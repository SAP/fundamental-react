import BusyIndicator from '../BusyIndicator';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import {
    boolean,
    select
} from '@storybook/addon-knobs';

<Meta title="Component API/BusyIndicator" component={BusyIndicator} />

# BusyIndicator

A **Busy Indicator** informs the user of an ongoing operation.

```javascript
 import { BusyIndicator } from 'fundamental-react/BusyIndicator';
 ```

## Examples

### Default
<Preview withToolbar>
  <Story name='Default'>
      <BusyIndicator show />
  </Story>
</Preview>

### Sizes

There are 3 sizes for Busy Indicator: `s`, `m` & `l`.

<Preview withToolbar>
  <Story name='Sizes'>
      <BusyIndicator show size='s' />
      <BusyIndicator show />
      <BusyIndicator show size='l' />
  </Story>
</Preview>

## Properties
<Props of={BusyIndicator} />


<!-- Stories excluded from documentation page -->

<Story name='Dev' parameters={{ docs: { disable: true }}}>
    <BusyIndicator
        show={boolean('show', true)}
        size={select('size', { 's': 's', 'm': 'm', 'l': 'l'})} />
</Story>
